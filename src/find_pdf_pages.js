const fs=require('fs'),path=require('path');
(async()=>{const [company,file,...terms]=process.argv.slice(2),pdfjs=await import('pdfjs-dist/legacy/build/pdf.mjs'),p=path.join(__dirname,'..','data','투자설명서',company,file),pdf=await pdfjs.getDocument({data:new Uint8Array(fs.readFileSync(p)),disableWorker:true}).promise;for(let n=1;n<=pdf.numPages;n++){const text=(await(await pdf.getPage(n)).getTextContent()).items.map(x=>x.str).join(' ').replace(/\s+/g,' ');const hits=terms.filter(t=>text.includes(t));if(hits.length)console.log(JSON.stringify({page:n,hits,text:text.slice(0,250)}))}})().catch(e=>{console.error(e);process.exitCode=1});
