const fs=require('fs'),path=require('path');
(async()=>{const [file,prefix,...nums]=process.argv.slice(2),pdfjs=await import('pdfjs-dist/legacy/build/pdf.mjs'),{createCanvas}=require('@napi-rs/canvas'),pdf=await pdfjs.getDocument({data:new Uint8Array(fs.readFileSync(file)),disableWorker:true}).promise;fs.mkdirSync(path.dirname(prefix),{recursive:true});for(const x of nums){const n=Number(x),page=await pdf.getPage(n),vp=page.getViewport({scale:1.7}),canvas=createCanvas(vp.width,vp.height),ctx=canvas.getContext('2d');await page.render({canvasContext:ctx,viewport:vp}).promise;fs.writeFileSync(`${prefix}-${n}.png`,canvas.toBuffer('image/png'));}})().catch(e=>{console.error(e);process.exitCode=1});
